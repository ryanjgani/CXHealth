<nav class="navbar navbar-expand-lg navbar-light bg-green">
    <div class="container-fluid container text-light">
      <a class="navbar-brand text-light" href="/">CX Health</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <div class="navbar-nav ">
                <a class="nav-link text-light" href="/dashboard">Dashboard</a>
                <a class="nav-link text-light" href="/rewards">Rewards</a>
                <% if (currentUser && currentUser.role === "patient") { %> 
                    <a class="nav-link" href="/patient/appointments/myappointments">My Appointments</a>
                <% } %> 
                <% if (currentUser && currentUser.role === "admin") { %>
                    <a class="nav-link" href="/admin/appointments/new">New Appointment</a>
                <% } %>  
            </div>
            <div class="navbar-nav ms-auto">
                <% if(!currentUser) { %> 
                <a data-bs-toggle="modal" data-bs-target="#loginModal" class="nav-link text-light">Login</a>
                <a data-bs-toggle="modal" data-bs-target="#registerModal" class="nav-link text-light">Register</a>
                <% } else { %> 
                <a href="/user/:id" class="text-light nav-link">Profile</a>
                <a href="/logout" class="text-light nav-link">Logout</a>
                <% } %> 
            </div>
        </div>
    </div>
  </nav>